Add your wedding music file here as:

  wedding-music.mp3

The floating music button in the site (bottom-right corner) is already wired
up to play/pause "/audio/wedding-music.mp3". Nothing will play until you add
a real file here — the button will simply do nothing (silently) if the file
is missing, which is expected.

Keep the file reasonably small (a 2-4 minute instrumental loop under ~5MB is
plenty) so it doesn't slow down the page on mobile data.
